{
    "name": "shareflow-addon",
    "version": "0.1.8.3",
    "description": "Premium article sharing extension for Firefox.",
    "scripts": {
        "start": "web-ext run",
        "build": "web-ext build --ignore-files $(tr '\n' ' ' < .webextignore)",
        "lint": "web-ext lint",
        "test": "npm run lint",
        "security:audit": "npm audit"
    },
    "devDependencies": {
        "web-ext": "^10.0.0"
    }
}
